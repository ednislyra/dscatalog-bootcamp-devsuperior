import './styles.css';

const CardLoader = () => {
  return (
    <main className="wrapper">
      <section className="loaders loaders-bg-4">
        <span className="loader loader-bars"> </span>
      </section>
    </main>
  );
};

export default CardLoader;
